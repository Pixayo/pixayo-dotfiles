{
  # Metadata for system configuration
  
  hostname = "nixos";
  system = "x86_64-linux"; # Change to match your system architecture

  # More users can be defined in configuration.nix
  default_user = "kaio"; # Change to your username
}